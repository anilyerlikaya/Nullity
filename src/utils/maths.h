#pragma once



// remove later
#include <iostream>

template <typename T> 
bool isBetween(T val, T start, T end);                                                                      /* Check if val is between start and end */   